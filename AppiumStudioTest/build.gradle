group 'com.experitest'
version '1.0-SNAPSHOT'

apply plugin: 'java'
apply plugin: 'eclipse'

sourceCompatibility = 1.8

repositories {
	mavenCentral()
	maven {
		url "https://cloud.experitest.com/repo/"
	}
}

eclipse {
    classpath {
        downloadJavadoc = true
    }
}

clean.dependsOn "cleanEclipse"
build.dependsOn "eclipse"

sourceSets {
    main {
        java {
            srcDirs = ['src']
        }
    }
}

dependencies {
	compile 'org.testng:testng:6.9.10'
	compile 'com.experitest:seetest-client:21.4'
	compile 'com.experitest:repo:+'
	
    implementation group: 'com.aventstack', name: 'extentreports', version: '5.0.8'

}

test {
	useTestNG() {
		suites './testng.xml'
	}
}
